[package]
name = "lvau-protocol"
version = "0.3.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
rand = "0.8"
rand_core = "0.6"
zeroize = { version = "1.8", features = ["derive", "serde"] }
